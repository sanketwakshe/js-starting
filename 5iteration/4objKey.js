
const object1 = {
    js:"javascript",
    cpp:"c++",
    c:'c programing'
 }
 for (const key in object1) {
    //console.log(object1[key]);
   // console.log(`${key} shortcut is for ${object1[key]}`);    //output all are in one sentance 1) js shortcut is javascript
    
 }

  const programing = [ 'c++','c','python']
  for (const key in programing) {
  console.log(programing[key]);
  
  }

  // for in loop not work on map

